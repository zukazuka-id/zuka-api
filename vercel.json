{
  "builds": [
    {
      "src": "src/vercel.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "**/.env*",
        "runtime": "nodejs22.x"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/vercel.ts" }
  ]
}
